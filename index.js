function divide() {
    return 2000 / 100;
}

const square = x => x * 2 // we get two because we're squaring it and the test requires 4 as our answer. " multiplies it times itself"


 const add = (a , b) => a + b



